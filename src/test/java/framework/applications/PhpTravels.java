package framework.applications;

import com.github.marcosbelfastdev.erbium.core.Driver;
import framework.core.browsers.single.BaseBrowser;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.remote.BrowserType;

public class PhpTravels extends BaseBrowser {

    public PhpTravels(String browserType) {
        super(browserType);
    }

    /*
    setBaseUrl(baseUrl)

    open(browserType)
    navigateToBaseUrl()
    centerBrowser()

    getBrowserConstrainer() -> returns a constrainer object
    exposeDriver();

     */

}
